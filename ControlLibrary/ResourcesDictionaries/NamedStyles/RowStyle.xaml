<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:Syncfusion="http://schemas.syncfusion.com/wpf"
                    xmlns:enums="clr-namespace:DataLayer.Enums;assembly=DataLayer">
    <Style x:Key="GenericRowStyle" TargetType="Syncfusion:VirtualizingCellsControl">
        <Setter Property="BorderBrush" Value="Gray" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Syncfusion:VirtualizingCellsControl">
                    <Grid>
                        <Rectangle Clip="{TemplateBinding RowBackgroundClip}" Fill="{TemplateBinding Background}" />
                        <Border Background="{TemplateBinding RowSelectionBrush}"
                                    BorderThickness="1"
                                    BorderBrush="DodgerBlue"
                                    Clip="{TemplateBinding SelectionBorderClipRect}"                                    
                                    Visibility="{TemplateBinding SelectionBorderVisiblity}" />
                        <Border BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter />
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="RdvRowStyle"  TargetType="Syncfusion:VirtualizingCellsControl" BasedOn="{StaticResource GenericRowStyle}">
        <Style.Triggers>
            <DataTrigger Binding="{Binding RdvStateValue}" Value="{x:Static enums:RdvState.Passed}">
                <Setter Property="Background" Value="DarkGray"></Setter>
                <Setter Property="Foreground" Value="LightGray"></Setter>
            </DataTrigger>
            <DataTrigger Binding="{Binding RdvStateValue}" Value="{x:Static enums:RdvState.Cancelled}">
                <Setter Property="Background" Value="LightSalmon"></Setter>
                <Setter Property="Foreground" Value="DarkOrange"></Setter>
            </DataTrigger>
        </Style.Triggers>
    </Style>
    <Style x:Key="PatientRowStyle"  TargetType="Syncfusion:VirtualizingCellsControl" BasedOn="{StaticResource GenericRowStyle}">
        <Style.Triggers>
            <DataTrigger Binding="{Binding Dead}" Value="True">
                <Setter Property="Background" Value="LightSalmon"></Setter>
                <Setter Property="Foreground" Value="Gray"></Setter>
            </DataTrigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>