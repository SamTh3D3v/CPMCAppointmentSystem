<UserControl x:Class="CPMCAppointmentSystem.UserControls.DragDrapRichTextBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="500">
    <Grid Background="WhiteSmoke">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ListView Name="LvNames"  BorderThickness="0" Background="Transparent" Margin="5"  ItemsSource="{Binding DragablePropertiesCollection}">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Focusable" Value="False"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate >
                    <Border  HorizontalAlignment="Stretch" BorderThickness="2" BorderBrush="Gray" MouseDown="LvNames_OnMouseDown" Style="{x:Null}" Margin="5" Tag="{Binding PropertyId}">
                        <StackPanel Orientation="Horizontal">
                            <Path Data="{StaticResource DragSignPath}" Stretch="Uniform" Fill="#FF000000" Width="22" Height="22" Margin="2" RenderTransformOrigin="0.5,0.5"/>
                            <TextBlock Text="{Binding PropertyName}" VerticalAlignment="Center" Style="{StaticResource SubheaderTextBlockStyle}" FontSize="18"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Border Grid.Column="1" Grid.Row="0" BorderThickness="2" BorderBrush="Gray" Margin="5" >
            <ScrollViewer VerticalScrollBarVisibility="Hidden" HorizontalScrollBarVisibility="Auto" >
                <TextBox PreviewDragEnter="UIElement_OnDragEnter"   
            PreviewDrop="UIElement_OnDrop">
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Setter Property="FontFamily" Value="Segoe UI"/>
                            <Setter Property="FontSize" Value="20"/>
                            <Setter Property="Foreground" Value="Black"/>
                            <Setter Property="HorizontalAlignment" Value="Stretch"/>
                            <Setter Property="VerticalAlignment" Value="Stretch"/>
                            <Setter Property="Background" Value="WhiteSmoke"/>
                            <Setter Property="AcceptsReturn" Value="True"/>
                            <Setter Property="Margin" Value="5"/>
                        </Style>
                    </TextBox.Style>
                </TextBox>
            </ScrollViewer>
        </Border>
        <Border Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="1" Background="DarkGray" >
            <TextBlock Text="glisser pour ajouter un paramètre" Style="{StaticResource SubheaderTextBlockStyle}" Margin="0,0,0,5" FontSize="20" Foreground="White" HorizontalAlignment="Center"/>
        </Border>
    </Grid>
</UserControl>
