<UserControl x:Class="CPMCAppointmentSystem.UserControls.DragDrapRichTextBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="500">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="180"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <ListView Name="LvNames"  BorderThickness="0" Background="Transparent" Margin="5"  ItemsSource="{Binding DragablePropertiesCollection}">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalAlignment" Value="Stretch"/>
                    <Setter Property="Focusable" Value="False"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border HorizontalAlignment="Stretch" MouseDown="LvNames_OnMouseDown" Style="{x:Null}" Margin="5" Tag="{Binding PropertyId}">
                        <StackPanel Orientation="Horizontal">
                            <Path Data="{StaticResource DragSignPath}" Stretch="Uniform" Fill="#FF000000" Width="25" Height="25" Margin="2" RenderTransformOrigin="0.5,0.5"/>
                            <TextBlock Text="{Binding PropertyName}"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Border Grid.Column="1" BorderThickness="2" BorderBrush="Gray" Margin="5" >
            <ScrollViewer VerticalScrollBarVisibility="Hidden" HorizontalScrollBarVisibility="Auto" >
                <TextBox PreviewDragEnter="UIElement_OnDragEnter"
            PreviewDrop="UIElement_OnDrop"
             Margin="5" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="WhiteSmoke" />
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
