<UserControl x:Class="CPMCAppointmentSystem.UserControls.TileItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:actions="clr-namespace:CPMCAppointmentSystem.Actions"
             mc:Ignorable="d" 
             d:DesignHeight="150" d:DesignWidth="310" x:Name="tileView">
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <actions:TileLoadedAction TargetObject="{Binding ElementName=tileView}"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <Grid Width="310" Height="150">     
        <Grid Background="{Binding Color}">
            <Grid.RenderTransform>
                <TransformGroup>
                    <ScaleTransform/>
                    <SkewTransform/>
                    <RotateTransform/>
                    <TranslateTransform/>
                </TransformGroup>
            </Grid.RenderTransform>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Image Source="{Binding Icon}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="10" Width="40" Height="40"
                                />
            <StackPanel Grid.Column="1" Margin="5 10" Grid.RowSpan="2">
                <TextBlock Text="{Binding Header}" FontFamily="Segoe UI" FontWeight="Light" FontSize="30" Foreground="White"/>
                <TextBlock Text="{Binding Description}" FontFamily="Segoe UI" FontWeight="Light" FontSize="20" Foreground="White" TextWrapping="Wrap"/>
            </StackPanel>                                               
        </Grid>
        <Grid   RenderTransformOrigin="0.5,0.5">
            <Grid.RenderTransform>
                <TransformGroup>
                    <ScaleTransform/>
                    <SkewTransform/>
                    <RotateTransform/>
                    <TranslateTransform Y="150"/>
                </TransformGroup>
            </Grid.RenderTransform>
            <Image Source="{Binding SlideImage}" Stretch="Fill"/>
        </Grid>
    </Grid>
</UserControl>
