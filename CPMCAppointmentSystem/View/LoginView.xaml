<Page x:Class="CPMCAppointmentSystem.View.LoginView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
      xmlns:system="clr-namespace:System;assembly=mscorlib"
      xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
      xmlns:command="http://www.galasoft.ch/mvvmlight"
      xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
      xmlns:resources="clr-namespace:CPMCAppointmentSystem.Resources"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="800" DataContext="{Binding Source={StaticResource Locator} , Path=LoginViewLodel}"
	Title="LoginView">
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <command:EventToCommand Command="{Binding Mode=OneWay, Path=LoginViewLoadedCommand}"
                            PassEventArgsToCommand="True" />
        </i:EventTrigger>
        <i:EventTrigger EventName="Unloaded">
            <command:EventToCommand Command="{Binding Mode=OneWay, Path=LoginViewUnLoadedCommand}"
                            PassEventArgsToCommand="True" />
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <Grid Background="{StaticResource BodyBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="45"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Background="{StaticResource HeaderBackgroundBrush}" Grid.Row="0" Grid.Column="0" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Image Source="{StaticResource SideLogo}"></Image>
            <TextBlock Text="{x:Static resources:WindowStaticText.Login_ApplicationTitleLabel}" Margin="5,0,0,5" Grid.Column="1" Style="{StaticResource HeaderTextBlockStyle}"  FontSize="17" Foreground="{StaticResource HeaderTextBrush}"></TextBlock>
        </Grid>
        <Border BorderThickness="5" BorderBrush="#44777777" Grid.Row="1" Background="{StaticResource InnerBodyBackgroundBrush}" Width="425" VerticalAlignment="Center" CornerRadius="5" >
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Text="{x:Static resources:WindowStaticText.Login_LoginLabel}" Style="{StaticResource SubheaderTextBlockStyle}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                <syncfusion:SfTextBoxExt Height="35"   Grid.Row="1" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Margin="10 5"                                      
                                     Watermark="{x:Static resources:WindowStaticText.Login_UserNameWaterMark}" VerticalContentAlignment="Center"  Text="{Binding UserName,Mode=TwoWay}" 
                                     >              
                </syncfusion:SfTextBoxExt>
                <PasswordBox Margin="10 5" VerticalContentAlignment="Center"  Height="35"  x:Name="PassBox" Style="{DynamicResource  WaterMarkPasswordBoxStyle}" Grid.Row="2" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
                    <PasswordBox.Resources>
                        <system:String x:Key="WaterMarkText">Mot de passe</system:String>
                    </PasswordBox.Resources>
                </PasswordBox>
                <Button Grid.Row="3" IsDefault="True" Command="{Binding LoginCommand}" CommandParameter="{Binding ElementName=PassBox}" Style="{StaticResource LoginButtonStyle}">
                    <Button.Content>
                        <StackPanel Orientation="Horizontal">
                            <Image Source="{StaticResource ConnectionLight}" Height="50"/>
                            <StackPanel Orientation="Vertical" Margin="5" VerticalAlignment="Center">
                                <TextBlock Text="{x:Static resources:WindowStaticText.Login_ConnectionButton}" VerticalAlignment="Center" Foreground="White" />
                                <!--<TextBlock Text="Randomly" FontSize="8" VerticalAlignment="Center" Foreground="Gainsboro" />-->
                            </StackPanel>
                        </StackPanel>
                    </Button.Content>
                </Button>
            </Grid>
        </Border>
        <controls:ProgressRing Grid.Row="1" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0,0,0,40" Foreground="{StaticResource AccentColorBrush}" IsActive="{Binding IsLogInProgressRingOn,Mode=TwoWay}"></controls:ProgressRing>
        <Image Grid.Row="1" VerticalAlignment="Bottom" HorizontalAlignment="Right" Source="{StaticResource ItCompRedLogo}" Stretch="Uniform" Margin="0,0,10,-20" Width="100" Height="100"/>

    </Grid>
</Page>
