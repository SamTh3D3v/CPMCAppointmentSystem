<UserControl x:Class="CPMCAppointmentSystem.View.StatisticsViews.PatientPerMedecinChartView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
                        d:DesignHeight="500" x:Name="RootPatientPerSexeUserControl" d:DesignWidth="920" DataContext="{Binding PatientsPerSexeChartViewModel,Source={StaticResource Locator}}">

    <Grid Background="LightSalmon">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Button  Grid.Column="2" Grid.Row="0" Margin="10" VerticalAlignment="Top" HorizontalAlignment="Right" RenderTransformOrigin="0.5, 0.5"
                Width="48" Height="48" Style="{StaticResource GoBackArrowButtonStyle}" Command="{Binding ReturnToOriginalTileCommand}" CommandParameter="{Binding ElementName=RootPatientPerSexeUserControl}">

            <Grid Background="Transparent">
                <Image Source="{StaticResource LeftArrowDark}"/>
            </Grid>
        </Button>

        <StackPanel Orientation="Horizontal" Grid.Column="0" Grid.Row="0" Margin="45 5" HorizontalAlignment="Left" VerticalAlignment="Center">
            <Image Source="{StaticResource ChartDark}" Width="55"/>
            <TextBlock Text="Patient par medecin" FontFamily="Segoe UI" FontWeight="Light" Foreground="White" FontSize="20" VerticalAlignment="Bottom" Margin="10 15 0 0"/>
        </StackPanel>
    </Grid>
</UserControl>