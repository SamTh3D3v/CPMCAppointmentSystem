<Page x:Class="CPMCAppointmentSystem.View.MainView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
      xmlns:command="http://www.galasoft.ch/mvvmlight"
      mc:Ignorable="d" 
      d:DesignHeight="700" d:DesignWidth="900"
	Title="MainView" DataContext="{Binding Source={StaticResource Locator},Path=MainViewModel}">
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <command:EventToCommand Command="{Binding Mode=OneWay, Path=MainViewLoadedCommand}"
                            PassEventArgsToCommand="True" />
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <Grid Background="{StaticResource BodyBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="45"/>
            <RowDefinition Height="*"/>            
        </Grid.RowDefinitions>
        <Grid Background="{StaticResource HeaderBackgroundBrush}" Grid.Row="0" Grid.Column="0" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>               
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Image Source="{StaticResource SideLogo}"/>
            <StackPanel Orientation="Horizontal" Grid.Column="1">
                <RadioButton GroupName="menu" Content="Calendar" Width="100" Command="{Binding CalendarCommand}" Style="{StaticResource HeaderToggleButtonStyle}"/>
                <RadioButton GroupName="menu" Content="Patients"  Width="100" Command="{Binding PatientsCommand}" Style="{StaticResource HeaderToggleButtonStyle}"/>
                <RadioButton GroupName="menu" Content="My Patients" Width="100" Command="{Binding MyPatientsCommand}" Style="{StaticResource HeaderToggleButtonStyle}"/>
                <RadioButton GroupName="menu" Content="Doctors"  Width="100" Command="{Binding DoctorsCommand}" Style="{StaticResource HeaderToggleButtonStyle}"/>
                <RadioButton GroupName="menu" Content="Specialities"  Width="100" Command="{Binding SpecialitiesCommand}" Style="{StaticResource HeaderToggleButtonStyle}"/>
                <RadioButton GroupName="menu" Content="Pathologies"  Width="100" Command="{Binding PathologiesCommand}" Style="{StaticResource HeaderToggleButtonStyle}"/>                
                <RadioButton GroupName="menu" Content="Statistics" Width="100" Command="{Binding StatisticsCommand}" Style="{StaticResource HeaderToggleButtonStyle}"/>
                <RadioButton GroupName="menu" Content="Settings" Width="100" Command="{Binding SettingsCommand}" Style="{StaticResource HeaderToggleButtonStyle}"/>
            </StackPanel>
            
        </Grid>        
        <Frame Grid.Row="1" x:Name="InnerFrame" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"/>        
    </Grid>
</Page>
